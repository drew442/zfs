# Codex Notes

This directory captures local context for the QAT/OpenZFS fork work.

The active feature branch is intended to add a fork-local `qat-deflate`
compression type and QAT deflate depth tuning. See `docs/qat-deflate.md` for
operator-facing behavior and compatibility notes.
